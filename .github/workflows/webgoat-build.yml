name: Build and Publish WebGoat
# This workflow builds the WebGoat project, scans it with JFrog Xray, and publishes the Docker image to Docker Hub.

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [ published ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK
      uses: actions/setup-java@v3
      with:
        java-version: '23'
        distribution: 'temurin'
        cache: maven
        
    - name: Build with Maven
      run: mvn -B clean install -Dmaven.compiler.release=17
      
    - name: Set up JFrog CLI
      uses: jfrog/setup-jfrog-cli@v3
      with:
        version: latest
      env:
        JF_URL: ${{ secrets.JF_URL }}
        JF_ACCESS_TOKEN: ${{ secrets.JF_ACCESS_TOKEN }}
        
    - name: JFrog Xray Scan
      run: |
        # Configure JFrog CLI
        jf c add xray-server --url=${JF_URL} --access-token=${JF_ACCESS_TOKEN}
        
        # Scan the project artifacts without specifying a project
        jf scan target/*.jar --fail=false
      env:
        JF_URL: ${{ secrets.JF_URL }}
        JF_ACCESS_TOKEN: ${{ secrets.JF_ACCESS_TOKEN }}
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
      
    - name: Login to Docker Hub
      if: github.event_name == 'release' || github.ref == 'refs/heads/main'
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        
    - name: Extract metadata for Docker
      id: meta
      uses: docker/metadata-action@v4
      with:
        images: ${{ secrets.DOCKERHUB_USERNAME }}/webgoat
        tags: |
          type=semver,pattern={{version}}
          type=ref,event=branch
          type=sha,format=short
          type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
          
    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      id: docker_build
      with:
        context: .
        push: ${{ github.event_name == 'release' || github.ref == 'refs/heads/main' }}
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        
    # Build local image for scanning if not pushed
    - name: Build local Docker image for scanning
      if: github.event_name != 'release' && github.ref != 'refs/heads/main'
      run: |
        docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/webgoat:local .
        
    - name: Scan Docker image with JFrog Xray
      run: |
        # Determine which image to scan
        if [[ "${{ github.event_name }}" == "release" || "${{ github.ref }}" == "refs/heads/main" ]]; then
          # Use the first tag from the pushed image
          IMAGE_NAME=$(echo ${{ steps.meta.outputs.tags }} | cut -d' ' -f1)
        else
          # Use the local image we just built
          IMAGE_NAME="${{ secrets.DOCKERHUB_USERNAME }}/webgoat:local"
        fi
        
        echo "Scanning image: ${IMAGE_NAME}"
        jf docker scan ${IMAGE_NAME} --fail=false
      env:
        JF_URL: ${{ secrets.JF_URL }}
        JF_ACCESS_TOKEN: ${{ secrets.JF_ACCESS_TOKEN }}
